/target/
Cargo.lock
**/*.rs.bk
.env
# Host specific
/.cargo/config.toml
# Written by the `generate-manifest` example: the full extracted driver tree, and the
# subset of assets to upload to a release. The committed manifest (download/src/manifest.rs)
# is the only output that belongs in git.
/drivers/
/release/
# Per-branch worktrees used for parallel agent work; each is its own git checkout,
# never content that belongs in this repo's tree.
/.claude/worktrees/
